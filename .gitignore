# ==========================================
# ENVIRONNEMENTS & CACHE PYTHON
# ==========================================
.conda/
venv/
env/
plastic_env/
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/

# ==========================================
# DONNÉES LOURDES & RÉSULTATS (Offline Edge)
# ==========================================
# Exclure le dataset d'images et labels
datasets/

# Exclure les poids des modèles lourds (.pt, .onnx, etc.)
models/
*.pt
*.onnx
*.engine

# Sorties d'entraînement (10/09/2026) : déménagées vers TRAINING_RUNS_DIR
# (E:\PixelOdyssey\6. Model outputs\runs, voir src/paths_config.py) - plus
# écrites ici en temps normal, mais exclusion large gardée en garde-fou (au
# cas où un script écrirait par erreur un output/ local : jamais du code,
# jamais à committer, quel que soit le nom/extension du fichier).
output/

*.html
*.geojson
*.tif
*.tiff

# Sorties de compare_runs.py --out-csv (regenerable, nettoyage du 08/09/2026 - etaient
# trackees par erreur alors que ce sont des resultats, pas du code)
/comparaison*.csv

# ==========================================
# EXCLUSIONS DIVERSES & OS
# ==========================================
.DS_Store
Thumbs.db
desktop.ini
*.zip
*.tar.gz

# ==========================================
# VS CODE (Garder la config partagée)
# ==========================================
.vscode/*
!.vscode/settings.json
!.vscode/launch.json